enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum RentalRequestStatus {
    PENDING
    APPROVED
    REJECTED
    ACTIVE
    COMPLETED
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}

enum PaymentProvider {
    STRIPE
    SSLCOMMERZ
}
